import Container from "@components/container";
import { Title } from "@components/typography";

function Blog() {
  return (
    <Container className="py-[50px] flex ">
      <div>
        <Title></Title>
      </div>
    </Container>
  );
}

export default Blog;
